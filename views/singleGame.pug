extends layout

block content
    div(class="single-game-container")
        h1=title 
        h3 Developed by         
            a(href=company.url) #{company.name}
        h2 Personal Rating: #{rating}
        h2 Brief Description: #{description}
        h2 Genre: #{genre.name}
        h2 Status: #{status}
        